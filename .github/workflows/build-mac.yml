name: Build Mac App

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install pyinstaller qrcode pillow reportlab
      
      - name: Build app
        run: |
          pyinstaller --onefile --windowed --name qr_code_generator qr_code_generator.py
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: qr-generator-mac
          path: dist/*.app/